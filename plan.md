# Plan (mark completed items with [x])

- [x] Integration: pipeline works without the `DPLYR` keyword (`tablename %>% select(column)` runs in DuckDB).
- [ ] Packaging: add an end-to-end test that loads the packed `dplyr_extension` from a local DuckDB repository and runs a simple query.
