name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  DUCKDB_PRIMARY_VERSION: v1.5.2
  DUCKDB_COMPAT_VERSION: v1.4.0

jobs:
  build-and-test-primary:
    name: Primary Support (${{ matrix.os }} / DuckDB ${{ matrix.duckdb_version }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            rust-toolchain: stable
            cmake-generator: "Unix Makefiles"
            duckdb_version: v1.5.2
            artifact-name: dplyr-v1.5.2-linux-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.5.2/duckdb_cli-linux-amd64.zip"
          - os: macos-14
            target: x86_64-apple-darwin
            rust-toolchain: stable
            cmake-generator: "Unix Makefiles"
            duckdb_version: v1.5.2
            artifact-name: dplyr-v1.5.2-macos-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.5.2/duckdb_cli-osx-universal.zip"
          - os: macos-latest
            target: aarch64-apple-darwin
            rust-toolchain: stable
            cmake-generator: "Unix Makefiles"
            duckdb_version: v1.5.2
            artifact-name: dplyr-v1.5.2-macos-arm64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.5.2/duckdb_cli-osx-universal.zip"
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            rust-toolchain: stable
            cmake-generator: "Ninja"
            duckdb_version: v1.5.2
            artifact-name: dplyr-v1.5.2-windows-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.5.2/duckdb_cli-windows-amd64.zip"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Pin DuckDB submodule to ${{ matrix.duckdb_version }}
        shell: bash
        run: |
          set -euo pipefail
          git -C duckdb fetch --tags --force
          git -C duckdb checkout "${{ matrix.duckdb_version }}"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust-toolchain }}
          targets: ${{ matrix.target }}

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          cache-on-failure: true

      - name: Install CMake
        uses: jwlawson/actions-setup-cmake@v1.14
        with:
          cmake-version: "3.20"

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libssl-dev \
            unzip \
            valgrind
          echo "VALGRIND_AVAILABLE=1" >> $GITHUB_ENV

      - name: Install macOS dependencies
        if: runner.os == 'macOS'
        run: |
          brew install pkg-config openssl
          echo "PKG_CONFIG_PATH=$(brew --prefix openssl)/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV

      - name: Install Rosetta (macOS x86_64 on ARM runners)
        if: runner.os == 'macOS' && matrix.target == 'x86_64-apple-darwin'
        shell: bash
        run: |
          sudo softwareupdate --install-rosetta --agree-to-license || true

      - name: Install Windows dependencies
        if: runner.os == 'Windows'
        run: |
          echo "Windows dependencies configured"

      - name: Setup MSVC toolchain (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Download and install DuckDB CLI
        shell: bash
        run: |
          mkdir -p duckdb-cli
          cd duckdb-cli

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            curl -L "${{ matrix.duckdb-url }}" -o duckdb.zip
            unzip -o duckdb.zip
            chmod +x duckdb.exe
            echo "$(pwd)" >> $GITHUB_PATH
          else
            curl -L "${{ matrix.duckdb-url }}" -o duckdb.zip
            rm -rf duckdb-bin
            mkdir -p duckdb-bin
            unzip -o duckdb.zip -d duckdb-bin
            chmod +x duckdb-bin/duckdb
            mv duckdb-bin/duckdb ./duckdb
            echo "$(pwd)" >> $GITHUB_PATH
          fi

      - name: Verify DuckDB installation
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            duckdb.exe --version
          else
            duckdb --version
          fi

      - name: Build Rust components
        run: |
          cargo build --manifest-path libdplyr_c/Cargo.toml --release --target ${{ matrix.target }}

      - name: Run Rust unit tests
        run: |
          cargo test --manifest-path libdplyr_c/Cargo.toml --release --target ${{ matrix.target }}

      - name: Configure CMake (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p build
          cd build

          OSX_ARCH=""
          LINUX_PLATFORM_FLAG=""
          LINUX_STATIC_FLAG=""
          if [[ "${{ runner.os }}" == "macOS" ]]; then
            case "${{ matrix.target }}" in
              x86_64-apple-darwin) OSX_ARCH="x86_64" ;;
              aarch64-apple-darwin) OSX_ARCH="arm64" ;;
            esac
          elif [[ "${{ runner.os }}" == "Linux" ]]; then
            if [[ "${{ matrix.target }}" == "aarch64-unknown-linux-gnu" ]]; then
              LINUX_PLATFORM_FLAG="-DDUCKDB_EXPLICIT_PLATFORM=linux_arm64"
            else
              LINUX_PLATFORM_FLAG="-DDUCKDB_EXPLICIT_PLATFORM=linux_amd64"
            fi
            LINUX_STATIC_FLAG="-DEXTENSION_STATIC_BUILD=ON"
          fi

          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_CPP_TESTS=ON \
            -DBUILD_DUCKDB=ON \
            ${LINUX_PLATFORM_FLAG} \
            ${LINUX_STATIC_FLAG} \
            -G "${{ matrix.cmake-generator }}" \
            -DRust_CARGO_TARGET="${{ matrix.target }}" \
            ${OSX_ARCH:+-DCMAKE_OSX_ARCHITECTURES=${OSX_ARCH}}

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        run: |
          if (-not (Test-Path -Path "build")) { New-Item -ItemType Directory -Path "build" | Out-Null }
          cd build
          cmake .. `
            -DCMAKE_BUILD_TYPE=Release `
            -DBUILD_CPP_TESTS=ON `
            -DBUILD_DUCKDB=ON `
            -DEXTENSION_STATIC_BUILD=ON `
            -G "${{ matrix.cmake-generator }}" `
            -DCMAKE_C_COMPILER=cl `
            -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_C_COMPILER_LAUNCHER="" `
            -DCMAKE_CXX_COMPILER_LAUNCHER="" `
            -DCCACHE_PROGRAM=OFF `
            -DRust_CARGO_TARGET="${{ matrix.target }}"

      - name: Create Swap Space
        if: runner.os == 'Linux'
        run: |
          sudo swapoff /swapfile 2>/dev/null || true
          sudo rm -f /swapfile
          sudo fallocate -l 8G /swapfile || sudo dd if=/dev/zero of=/swapfile bs=1M count=8192
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          sudo swapon --show

      - name: Build extension
        run: |
          cd build
          cmake --build . --config Release --parallel 1

      - name: Run smoke tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          export BUILD_DIR=build
          chmod +x tests/run_smoke_tests.sh
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ./tests/run_smoke_tests.sh
          else
            ./tests/run_smoke_tests.sh
          fi

      - name: Run smoke tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          set PATH=%CD%\duckdb-cli;%PATH%
          set BUILD_DIR=build
          tests\run_smoke_tests.bat

      - name: Run C++ integration tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd build
          export DUCKDB_EXTENSION_PATH=$(pwd)
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ./duckdb_extension_integration_test --gtest_color=yes
          else
            ./duckdb_extension_integration_test --gtest_color=yes
          fi

      - name: Run C++ integration tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          cd build
          set DUCKDB_EXTENSION_PATH=%CD%
          set TEST_EXE=
          for /r %%f in (duckdb_extension_integration_test.exe) do (
            set TEST_EXE=%%f
            goto :found_test_exe
          )

          :found_test_exe
          if not defined TEST_EXE (
            echo Error: duckdb_extension_integration_test.exe not found
            echo Available test executables:
            dir /s /b *.exe
            exit /b 1
          )
          echo Found test executable: %TEST_EXE%
          "%TEST_EXE%" --gtest_color=yes

      - name: Run CMake tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd build
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ctest --output-on-failure --timeout 300
          else
            ctest --output-on-failure --timeout 300
          fi

      - name: Run CMake tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          set PATH=%CD%\duckdb-cli;%PATH%
          cd build
          ctest -C Release --output-on-failure --timeout 300

      - name: Memory leak check (Linux only)
        if: runner.os == 'Linux' && env.VALGRIND_AVAILABLE == '1'
        run: |
          cd build
          export DUCKDB_EXTENSION_PATH=$(pwd)
          valgrind --tool=memcheck --leak-check=full --error-exitcode=1 \
            duckdb -unsigned :memory: -c "LOAD './dplyr.duckdb_extension'; SELECT 'Memory check passed' as result;"

      - name: Collect build artifacts (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p artifacts
          cp build/dplyr.duckdb_extension artifacts/
          cp libdplyr_c/target/${{ matrix.target }}/release/libdplyr_c.a artifacts/ || true

          cat > artifacts/build-info.json << EOF
          {
            "platform": "${{ matrix.os }}",
            "target": "${{ matrix.target }}",
            "duckdb_version": "${{ matrix.duckdb_version }}",
            "support_tier": "primary",
            "rust_version": "$(rustc --version)",
            "cmake_version": "$(cmake --version | head -n1)",
            "build_timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_commit": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}"
          }
          EOF

      - name: Collect build artifacts (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          mkdir -p artifacts
          EXT_BIN=$(find build -name "dplyr.duckdb_extension" -type f | head -n 1)
          if [[ -n "$EXT_BIN" ]]; then
            echo "Found extension binary: $EXT_BIN"
            cp "$EXT_BIN" artifacts/
          else
            echo "Error: dplyr.duckdb_extension not found"
            find build -name "*.duckdb_extension"
            exit 1
          fi
          cp libdplyr_c/target/${{ matrix.target }}/release/dplyr_c.lib artifacts/ || true

          cat > artifacts/build-info.json << EOF
          {
            "platform": "${{ matrix.os }}",
            "target": "${{ matrix.target }}",
            "duckdb_version": "${{ matrix.duckdb_version }}",
            "support_tier": "primary",
            "rust_version": "$(rustc --version)",
            "cmake_version": "$(cmake --version | head -n1)",
            "build_timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_commit": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}"
          }
          EOF

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: artifacts/
          retention-days: 30

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.artifact-name }}
          path: |
            build/Testing/
            *.log
            test-*.xml
          retention-days: 7

  compatibility-check:
    name: Compatibility Check (${{ matrix.os }} / DuckDB v1.4.0)
    if: github.event_name != 'pull_request' || matrix.os == 'ubuntu-latest'
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            cmake-generator: "Unix Makefiles"
            artifact-name: dplyr-v1.4.0-compat-linux-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.4.0/duckdb_cli-linux-amd64.zip"
          - os: macos-14
            target: x86_64-apple-darwin
            cmake-generator: "Unix Makefiles"
            artifact-name: dplyr-v1.4.0-compat-macos-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.4.0/duckdb_cli-osx-universal.zip"
          - os: macos-latest
            target: aarch64-apple-darwin
            cmake-generator: "Unix Makefiles"
            artifact-name: dplyr-v1.4.0-compat-macos-arm64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.4.0/duckdb_cli-osx-universal.zip"
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            cmake-generator: "Ninja"
            artifact-name: dplyr-v1.4.0-compat-windows-x86_64
            duckdb-url: "https://github.com/duckdb/duckdb/releases/download/v1.4.0/duckdb_cli-windows-amd64.zip"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Pin DuckDB submodule to compatibility version
        shell: bash
        run: |
          set -euo pipefail
          git -C duckdb fetch --tags --force
          git -C duckdb checkout "${{ env.DUCKDB_COMPAT_VERSION }}"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: compat-${{ matrix.target }}
          cache-on-failure: true

      - name: Install CMake
        uses: jwlawson/actions-setup-cmake@v1.14
        with:
          cmake-version: "3.20"

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libssl-dev \
            unzip \
            valgrind
          echo "VALGRIND_AVAILABLE=1" >> $GITHUB_ENV

      - name: Install macOS dependencies
        if: runner.os == 'macOS'
        run: |
          brew install pkg-config openssl
          echo "PKG_CONFIG_PATH=$(brew --prefix openssl)/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV

      - name: Install Rosetta (macOS x86_64 on ARM runners)
        if: runner.os == 'macOS' && matrix.target == 'x86_64-apple-darwin'
        shell: bash
        run: |
          sudo softwareupdate --install-rosetta --agree-to-license || true

      - name: Install Windows dependencies
        if: runner.os == 'Windows'
        run: |
          echo "Windows dependencies configured"

      - name: Setup MSVC toolchain (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Download and install DuckDB CLI
        shell: bash
        run: |
          mkdir -p duckdb-cli
          cd duckdb-cli

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            curl -L "${{ matrix.duckdb-url }}" -o duckdb.zip
            unzip -o duckdb.zip
            chmod +x duckdb.exe
            echo "$(pwd)" >> $GITHUB_PATH
          else
            curl -L "${{ matrix.duckdb-url }}" -o duckdb.zip
            rm -rf duckdb-bin
            mkdir -p duckdb-bin
            unzip -o duckdb.zip -d duckdb-bin
            chmod +x duckdb-bin/duckdb
            mv duckdb-bin/duckdb ./duckdb
            echo "$(pwd)" >> $GITHUB_PATH
          fi

      - name: Verify DuckDB installation
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            duckdb.exe --version
          else
            duckdb --version
          fi

      - name: Build Rust components
        run: |
          cargo build --manifest-path libdplyr_c/Cargo.toml --release --target ${{ matrix.target }}

      - name: Run Rust unit tests
        run: |
          cargo test --manifest-path libdplyr_c/Cargo.toml --release --target ${{ matrix.target }}

      - name: Configure CMake (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p build
          cd build

          OSX_ARCH=""
          LINUX_PLATFORM_FLAG=""
          LINUX_STATIC_FLAG=""
          if [[ "${{ runner.os }}" == "macOS" ]]; then
            case "${{ matrix.target }}" in
              x86_64-apple-darwin) OSX_ARCH="x86_64" ;;
              aarch64-apple-darwin) OSX_ARCH="arm64" ;;
            esac
          elif [[ "${{ runner.os }}" == "Linux" ]]; then
            LINUX_PLATFORM_FLAG="-DDUCKDB_EXPLICIT_PLATFORM=linux_amd64"
            LINUX_STATIC_FLAG="-DEXTENSION_STATIC_BUILD=ON"
          fi

          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_CPP_TESTS=ON \
            -DBUILD_DUCKDB=ON \
            ${LINUX_PLATFORM_FLAG} \
            ${LINUX_STATIC_FLAG} \
            -G "${{ matrix.cmake-generator }}" \
            -DRust_CARGO_TARGET="${{ matrix.target }}" \
            ${OSX_ARCH:+-DCMAKE_OSX_ARCHITECTURES=${OSX_ARCH}}

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        run: |
          if (-not (Test-Path -Path "build")) { New-Item -ItemType Directory -Path "build" | Out-Null }
          cd build
          cmake .. `
            -DCMAKE_BUILD_TYPE=Release `
            -DBUILD_CPP_TESTS=ON `
            -DBUILD_DUCKDB=ON `
            -DEXTENSION_STATIC_BUILD=ON `
            -G "${{ matrix.cmake-generator }}" `
            -DCMAKE_C_COMPILER=cl `
            -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_C_COMPILER_LAUNCHER="" `
            -DCMAKE_CXX_COMPILER_LAUNCHER="" `
            -DCCACHE_PROGRAM=OFF `
            -DRust_CARGO_TARGET="${{ matrix.target }}"

      - name: Create Swap Space
        if: runner.os == 'Linux'
        run: |
          sudo swapoff /swapfile 2>/dev/null || true
          sudo rm -f /swapfile
          sudo fallocate -l 8G /swapfile || sudo dd if=/dev/zero of=/swapfile bs=1M count=8192
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          sudo swapon --show

      - name: Build extension
        run: |
          cd build
          cmake --build . --config Release --parallel 1

      - name: Run smoke tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          export BUILD_DIR=build
          chmod +x tests/run_smoke_tests.sh
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ./tests/run_smoke_tests.sh
          else
            ./tests/run_smoke_tests.sh
          fi

      - name: Run smoke tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          set PATH=%CD%\duckdb-cli;%PATH%
          set BUILD_DIR=build
          tests\run_smoke_tests.bat

      - name: Run C++ integration tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd build
          export DUCKDB_EXTENSION_PATH=$(pwd)
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ./duckdb_extension_integration_test --gtest_color=yes
          else
            ./duckdb_extension_integration_test --gtest_color=yes
          fi

      - name: Run C++ integration tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          cd build
          set DUCKDB_EXTENSION_PATH=%CD%
          set TEST_EXE=
          for /r %%f in (duckdb_extension_integration_test.exe) do (
            set TEST_EXE=%%f
            goto :found_test_exe
          )

          :found_test_exe
          if not defined TEST_EXE (
            echo Error: duckdb_extension_integration_test.exe not found
            echo Available test executables:
            dir /s /b *.exe
            exit /b 1
          )
          echo Found test executable: %TEST_EXE%
          "%TEST_EXE%" --gtest_color=yes

      - name: Run CMake tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd build
          if [[ "${{ runner.os }}" == "macOS" && "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            arch -x86_64 ctest --output-on-failure --timeout 300
          else
            ctest --output-on-failure --timeout 300
          fi

      - name: Run CMake tests (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          set PATH=%CD%\duckdb-cli;%PATH%
          cd build
          ctest -C Release --output-on-failure --timeout 300

      - name: Memory leak check
        if: runner.os == 'Linux' && env.VALGRIND_AVAILABLE == '1'
        run: |
          cd build
          export DUCKDB_EXTENSION_PATH=$(pwd)
          valgrind --tool=memcheck --leak-check=full --error-exitcode=1 \
            duckdb -unsigned :memory: -c "LOAD './dplyr.duckdb_extension'; SELECT 'Memory check passed' as result;"

      - name: Collect build artifacts (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p artifacts
          cp build/dplyr.duckdb_extension artifacts/
          cp libdplyr_c/target/${{ matrix.target }}/release/libdplyr_c.a artifacts/ || true

          cat > artifacts/build-info.json << EOF
          {
            "platform": "${{ matrix.os }}",
            "target": "${{ matrix.target }}",
            "duckdb_version": "${{ env.DUCKDB_COMPAT_VERSION }}",
            "support_tier": "compatibility",
            "rust_version": "$(rustc --version)",
            "cmake_version": "$(cmake --version | head -n1)",
            "build_timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_commit": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}"
          }
          EOF

      - name: Collect build artifacts (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          mkdir -p artifacts
          EXT_BIN=$(find build -name "dplyr.duckdb_extension" -type f | head -n 1)
          if [[ -n "$EXT_BIN" ]]; then
            echo "Found extension binary: $EXT_BIN"
            cp "$EXT_BIN" artifacts/
          else
            echo "Error: dplyr.duckdb_extension not found"
            find build -name "*.duckdb_extension"
            exit 1
          fi
          cp libdplyr_c/target/${{ matrix.target }}/release/dplyr_c.lib artifacts/ || true

          cat > artifacts/build-info.json << EOF
          {
            "platform": "${{ matrix.os }}",
            "target": "${{ matrix.target }}",
            "duckdb_version": "${{ env.DUCKDB_COMPAT_VERSION }}",
            "support_tier": "compatibility",
            "rust_version": "$(rustc --version)",
            "cmake_version": "$(cmake --version | head -n1)",
            "build_timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_commit": "${{ github.sha }}",
            "git_ref": "${{ github.ref }}"
          }
          EOF

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: artifacts/
          retention-days: 30

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.artifact-name }}
          path: |
            build/Testing/
            *.log
            test-*.xml
          retention-days: 7

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Install additional tools
        run: |
          cargo install cargo-audit cargo-deny

      - name: Check Rust formatting
        run: cargo fmt --all -- --check

      - name: Run Rust clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Security audit
        run: cargo audit

      - name: License and dependency check
        run: cargo deny check

      - name: Documentation check
        run: cargo doc --manifest-path libdplyr_c/Cargo.toml --no-deps --document-private-items
